numericVector <- numeric(200) # to initialize a numeric vector with length 200 elements
Vec_N <- c(100, 200, 300, 400, 500)  # numeric vector
print(Vec_N)
Vec_N[-1]  # returns all elements except in position 1.
print(Vec_N[-1])
Vec_CH <- c("a", "b", "c", "d", "e")  # character vector
print(Vec_CH)
Vec_Logic <- c(TRUE, FALSE, TRUE, TRUE, TRUE)  # logical vector
vec4 <- gl(4, 1, 4, label = c("l1", "l2", "l3", "l4"))  # factor with 4 levels
length(Vec_N )  # 5
print(Vec_N [1])  # Access to the first element
print(Vec_N [1:3]) # Access to the three first elements
